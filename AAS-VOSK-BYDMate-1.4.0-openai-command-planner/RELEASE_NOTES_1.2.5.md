# AAS-VOSK-BYDMate 1.2.5

- Импортированы все 50 явных WriteEntry из предоставленного WriteAllowlist.kt.
- Добавлена строго allowlist-ограниченная команда `BYDMate <action_name> [value]`.
- Добавлено управление положением каждого окна и групп окон в процентах 0–100.
- Диапазон уровней обогрева/вентиляции передних сидений расширен до 1–5.
- Исправлен код выключения прямого seat switch: 2 вместо 0, согласно исходному WriteAllowlist.
- Команды окон, люка, шторки и багажника требуют подтверждённой нулевой скорости.
- Оригинальный файл сохранён в `reference/WriteAllowlist.kt`.

Важно: комментарий исходного файла упоминает ещё 123 команды из отдельного
`competitor-actions.json`. Сам JSON не был предоставлен, поэтому эти невидимые
записи не добавлялись и не угадывались.
